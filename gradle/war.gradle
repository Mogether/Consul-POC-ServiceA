apply plugin: "war"

bootWar {
    mainClass = "com.poc.servicea.ServiceAApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
